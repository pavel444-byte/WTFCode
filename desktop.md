### Instructions To run desktop
- Make sure you have all dependencies installed (and library too). Run 'uv sync' if you haven't already.

- Run the desktop application using the command `WTFdesktop` in your terminal.