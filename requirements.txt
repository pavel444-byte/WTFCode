# Runtime dependencies for Python 3.13.
# Keep this file focused on direct dependencies so GitHub Actions can resolve
# compatible transitive versions for the runner platform.
openai>=1.0.0
anthropic>=0.34.0
google-genai>=1.0.0
rich>=13.0.0
python-dotenv>=1.0.0
requests>=2.32.0
streamlit>=1.41.0
pyyaml>=6.0.3
customtkinter>=5.2.2
Pillow>=11.0.0

# Optional Windows-only notification/window helpers. These must not be installed
# on Linux GitHub Actions runners because their winrt dependencies are Windows-only.
win11toast>=0.36.3; sys_platform == "win32"
pygetwindow>=0.0.9; sys_platform == "win32"
